---
title: 시작하기
description: Kotlin 백엔드 프로젝트에 bluetape4k 의존성 거버넌스를 추가하고 첫 모듈을 선택합니다.
---

## 요구 사항

- Kotlin 2.3+
- 대상 repository에 따라 Java 21 또는 Java 25
- Gradle Kotlin DSL
- Framework integration을 사용하는 경우 Spring Boot 3/4 또는 Ktor 3

## 현재 BOM 추가

Gradle Kotlin DSL에서는 `bluetape4k-dependencies` version만 version catalog에
두고, bluetape4k module alias에는 version을 쓰지 않습니다.

```toml
[versions]
bluetape4k-dependencies = "1.2.0"

[libraries]
bluetape4k-dependencies = { module = "io.github.bluetape4k:bluetape4k-dependencies", version.ref = "bluetape4k-dependencies" }
bluetape4k-core = { module = "io.github.bluetape4k:bluetape4k-core" }
bluetape4k-coroutines = { module = "io.github.bluetape4k:bluetape4k-coroutines" }
```

중앙 BOM은 공유 third-party version과 bluetape4k 모듈 version을 관리합니다.
애플리케이션의 module dependency에는 version을 쓰지 않습니다.

```kotlin
dependencies {
    implementation(platform(libs.bluetape4k.dependencies))
    implementation(libs.bluetape4k.core)
    implementation(libs.bluetape4k.coroutines)
}
```

Repository-specific module을 사용할 때도 규칙은 같습니다. 중앙 BOM은 그대로 두고,
서비스에 필요한 module만 추가합니다.

```toml
[libraries]
exposed-jdbc = { module = "io.github.bluetape4k.exposed:bluetape4k-exposed-jdbc" }
bluetape4k-aws = { module = "io.github.bluetape4k.aws:bluetape4k-aws-spring-boot" }
```

```kotlin
dependencies {
    implementation(platform(libs.bluetape4k.dependencies))
    implementation(libs.exposed.jdbc)
    implementation(libs.bluetape4k.aws)
}
```

현재 dependency governance 기준은 [버전 거버넌스](/ko/ecosystem/version-governance/)에서
확인할 수 있습니다.

## 시작 지점 선택

| 목표 | 시작할 곳 |
| --- | --- |
| 공유 버전 거버넌스 | [`bluetape4k-dependencies`](https://github.com/bluetape4k/bluetape4k-dependencies) |
| Core Kotlin과 Spring Boot backend utility | [`bluetape4k-projects`](https://github.com/bluetape4k/bluetape4k-projects) |
| Exposed JDBC/R2DBC repository | [`bluetape4k-exposed`](https://github.com/bluetape4k/bluetape4k-exposed) |
| AWS SDK v2와 AWS Kotlin SDK wrapper | [`bluetape4k-aws`](https://github.com/bluetape4k/bluetape4k-aws) |
| Graph database integration | [`bluetape4k-graph`](https://github.com/bluetape4k/bluetape4k-graph) |
| Distributed leader election과 runtime coordination | [`bluetape4k-leader`](https://github.com/bluetape4k/bluetape4k-leader) |
| 한국어/일본어 tokenizer와 text processing | [`bluetape4k-text`](https://github.com/bluetape4k/bluetape4k-text) |
| Backend image processing | [`bluetape4k-image`](https://github.com/bluetape4k/bluetape4k-image) |
| Audit history와 entity diff workflow | [`bluetape4k-javers`](https://github.com/bluetape4k/bluetape4k-javers) |
| Platform migration과 incubation work | [`bluetape4k-experimental`](https://github.com/bluetape4k/bluetape4k-experimental) |
| 일반 end-to-end 예제 | [`bluetape4k-workshop`](https://github.com/bluetape4k/bluetape4k-workshop) |
| Exposed JDBC 예제 | [`exposed-workshop`](https://github.com/bluetape4k/exposed-workshop) |
| Exposed R2DBC 예제 | [`exposed-r2dbc-workshop`](https://github.com/bluetape4k/exposed-r2dbc-workshop) |
| Optimization 예제 | [`timefold-workshop`](https://github.com/bluetape4k/timefold-workshop) |
| Reference application | [`clinic-appointment`](https://github.com/bluetape4k/clinic-appointment) |

## 프로젝트 검증

```bash
./gradlew compileTestKotlin --no-daemon
```

Dependency surface를 넓히기 전에 compile check를 먼저 실행합니다. Container 기반 module은 compile이 성공한 뒤 focused integration test를 실행합니다.
