---
title: "Bluetape4k Projects Part 1: The Shared Kotlin/JVM Foundation"
description: Why bluetape4k-projects is the shared foundation of the bluetape4k ecosystem, what stays inside it, and what moves to standalone repositories.
sidebar:
  order: -202605301630
blog:
  date: 2026-05-30T16:30:00+09:00
  image: /assets/bluetape4k-projects-part1-hero.png
  imageAlt: Small robotic builders assembling Kotlin JVM backend module blocks around a shared foundation platform
  cardDescription: "Why core, coroutines, I/O, data, infra, Spring Boot, Ktor, testing, and the BOM live together."
---

<figure class="bt4k-blog-hero">
  <img src="/assets/bluetape4k-projects-part1-hero.png" alt="Small robotic builders assembling Kotlin JVM backend module blocks around a shared foundation platform" loading="eager" />
  <figcaption>A shared foundation is not glamorous. It just keeps every service from rebuilding the same floor.</figcaption>
</figure>

<p class="bt4k-post-meta">2026-05-30 · bluetape4k-projects series · Part 1</p>

This is Part 1 of the `bluetape4k-projects` series. The full series continues with
[Part 1: shared foundation](/blog/bluetape4k-projects-part1-shared-foundation/), then Part 2:
core, coroutines, logging, tests, Part 3: I/O, serialization, HTTP, encryption, Part 4: data and
infrastructure, Part 5: utilities and adoption path, and Part 6: Spring Boot 4 and Ktor.

`bluetape4k-projects` is the broadest foundation in the bluetape4k ecosystem. It collects the
Kotlin/JVM utilities, coroutine helpers, I/O wrappers, data and infrastructure adapters, testing
support, and BOM that backend services repeatedly need.

It is not a place where every feature stays forever. AWS, Exposed, Image, Text, Leader, and
JaVers have moved into standalone repositories because they carry their own domain depth and
release rhythm. `projects` keeps the shared foundation and runtime building blocks that many
repositories and services depend on first.

<figure class="bt4k-architecture">
  <img src="/assets/bluetape4k-projects-overview-01.png" alt="Bluetape4k Projects overview diagram with BOM, foundation, runtime adapters, applications, and split repositories" loading="lazy" />
  <figcaption>`projects` starts from the BOM and foundation, then supports runtime adapters and application code.</figcaption>
</figure>

## Why This Repository Exists

Kotlin/JVM backend work usually starts on proven Java libraries. The rough edges appear when those
libraries meet Kotlin service code.

- Java APIs often do not fit Kotlin null-safety or DSL style.
- Callbacks, `Future`, and blocking APIs do not always compose cleanly with coroutines.
- Tests repeatedly need Redis, Kafka, databases, clocks, concurrency, and integration fixtures.
- Services re-create similar codec, serializer, timeout, retry, and logging conventions.

`bluetape4k-projects` reduces that repetition. It is less a new all-encompassing framework and
more a thin Kotlin-friendly layer over the libraries already used in production systems.

```kotlin
implementation(platform("io.github.bluetape4k:bluetape4k-bom:<version>"))
implementation("io.github.bluetape4k:bluetape4k-core")
implementation("io.github.bluetape4k:bluetape4k-coroutines")
implementation("io.github.bluetape4k:bluetape4k-junit5")
```

The root README describes the same direction: idiomatic Kotlin helpers, Java library wrappers,
coroutine integration, testing infrastructure, Redis/cache/infra support, and split repositories
for larger domain areas.

## Module Map

The current root README shows the boundary clearly.

<figure class="bt4k-architecture">
  <img src="/assets/bluetape4k-projects-module-map-01.png" alt="Bluetape4k Projects module structure diagram grouped by core, I/O, data, infra, cache, Spring Boot, Ktor, utilities, testing, and examples" loading="lazy" />
  <figcaption>The root README module map shows `projects` as both foundation and runtime adapter layer.</figcaption>
</figure>

| Area | Representative modules | Adoption trigger |
|---|---|---|
| Foundation | `core`, `coroutines`, `logging`, `bom` | Kotlin style, execution model, and dependency alignment |
| I/O | `io`, `okio`, `csv`, `protobuf`, `jackson2/3`, `http`, `grpc` | Bytes, files, serializers, HTTP, and RPC boundaries |
| Data | `jdbc`, `r2dbc`, `hibernate`, `mongodb`, `cassandra` | Persistence helpers and coroutine data access |
| Infrastructure | `lettuce`, `redisson`, `kafka`, `nats`, `opentelemetry`, `resilience4j` | Adapters around operational systems |
| Application | `spring-boot/*`, `ktor/*`, `examples/*` | Service entrypoints and runnable examples |
| Testing | `assertions`, `junit5`, `testcontainers` | Reusable integration-test fixtures |

The important point is not "pull in the whole ecosystem." The model is selective adoption. The
BOM aligns versions, and each module can be chosen for the boundary the service actually touches.

## What Moves Out

As a feature area grows, keeping it inside `projects` can slow it down. bluetape4k splits those
areas into standalone repositories.

| Standalone repository | Why it moved out |
|---|---|
| `bluetape4k-aws` | AWS SDK v2 and Kotlin SDK service coverage has its own release cadence |
| `bluetape4k-exposed` | Exposed 1.2.x repositories, cache, and SQL DSL form their own ecosystem |
| `bluetape4k-image` | Pure JVM image processing and libvips/JNI/FFM need separate validation |
| `bluetape4k-text` | Korean/Japanese tokenizers and text search need their own dictionaries and quality bars |
| `bluetape4k-leader` | Leader election API and backend parity evolve as a focused subsystem |
| `bluetape4k-javers` | Audit/diff persistence spans Redis, Kafka, and Exposed on a separate release axis |

The rough rule is simple: thin shared Kotlin/JVM foundation stays in `projects`; a domain with its
own learning curve, release rhythm, and verification surface moves out.

## What the BOM Does

The BOM looks small, but it matters in a shared foundation repository. Kotlin, Spring Boot,
Exposed, Jackson, Redis clients, and testing libraries all move at different speeds.

`bluetape4k-bom` aligns the module versions published from `projects`. Applications import the
BOM first, then declare only the modules they need.

```kotlin
dependencies {
    implementation(platform("io.github.bluetape4k:bluetape4k-bom:<version>"))
    implementation("io.github.bluetape4k:bluetape4k-okio")
    implementation("io.github.bluetape4k:bluetape4k-r2dbc")
    testImplementation("io.github.bluetape4k:bluetape4k-testcontainers")
}
```

For Gradle Kotlin DSL, the root README recommends `implementation(platform(...))` over
`dependencyManagement { imports }` to avoid conflicts with KGP 2.3.x.

## Where to Start

For a first pass through the repository, this order is usually the least noisy.

1. Read the root `README.md` for the module map.
2. Skim `core`, `coroutines`, `logging`, `junit5`, and `testcontainers`.
3. Pick I/O, data, or infrastructure modules based on the boundary your service actually touches.
4. Use Spring Boot or Ktor examples to see application-level wiring.
5. Follow topic posts for cache, virtual threads, CSV/Okio, and benchmark-heavy areas when needed.

<figure class="bt4k-architecture">
  <img src="/assets/bluetape4k-projects-module-chart-01.png" alt="Bluetape4k Projects module composition chart" loading="lazy" />
  <figcaption>When a repository has many modules, first look at the shape, then pick the slice you need.</figcaption>
</figure>

## Source Links

- Issue: [#80 Blog: bluetape4k-projects Part 1 - the shared foundation](https://github.com/bluetape4k/bluetape4k.github.io/issues/80)
- Series epic: [#79 Blog series: bluetape4k-projects shared Kotlin/JVM foundation guide](https://github.com/bluetape4k/bluetape4k.github.io/issues/79)
- Repository: [bluetape4k-projects](https://github.com/bluetape4k/bluetape4k-projects)
- README: [README.md](https://github.com/bluetape4k/bluetape4k-projects/blob/develop/README.md)
- Korean README: [README.ko.md](https://github.com/bluetape4k/bluetape4k-projects/blob/develop/README.ko.md)
- BOM README: [bluetape4k/bom/README.md](https://github.com/bluetape4k/bluetape4k-projects/blob/develop/bluetape4k/bom/README.md)
- Cache deep dive: [Bluetape4k Cache Part 1](/blog/bluetape4k-cache-part1-cache-module/)

## Closing

In one sentence, `bluetape4k-projects` is the floor Kotlin/JVM backend services should not have
to rebuild each time. Large domain capabilities move into focused repositories, while this repo
keeps core utilities, coroutines, I/O, data, infrastructure, application support, testing, and
the BOM aligned.

Next, we will open the innermost layer: `core`, `coroutines`, `logging`, and testing modules. The
small foundations are not exciting until they are missing. Then they become the whole incident
review.

## Series Links

- [Part 1: shared Kotlin/JVM foundation](/blog/bluetape4k-projects-part1-shared-foundation/)
- Part 2: core, coroutines, logging, tests (planned)
- Part 3: I/O, serialization, HTTP, encryption (planned)
- Part 4: data and infrastructure (planned)
- Part 5: utilities and adoption path (planned)
- Part 6: Spring Boot 4 and Ktor (planned)
