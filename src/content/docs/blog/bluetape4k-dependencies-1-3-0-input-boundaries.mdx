---
title: "bluetape4k-dependencies 1.3.0 in Practice Part 4: Input Boundaries and Resource Safety"
description: "How to bound input size, memory allocation, processing time, and failure responses across Image OCR, large-file I/O, tokenizers, and blockword APIs."
sidebar:
  order: -202606281400
blog:
  date: 2026-06-28T14:00:00+09:00
  image: /assets/bluetape4k-dependencies-1-3-0-hero.png
  imageAlt: Small robotic workers inspect projects, exposed, aws, image, text, leader, graph, and javers module blocks beside a dependency BOM board in a 3D workbench illustration
  cardDescription: "Input boundaries often pass small samples and fail on production data. Image and Text changes are easiest to understand from the API edge."
  tags: ["dependencies","image","kotlin","practical-example","search","text"]
---

<figure class="bt4k-blog-hero">
  <img src="/assets/bluetape4k-dependencies-1-3-0-hero.png" alt="Small robotic workers inspect projects, exposed, aws, image, text, leader, graph, and javers module blocks beside a dependency BOM board in a 3D workbench illustration" loading="eager" />
  <figcaption>Input-boundary bugs usually start with large files, long strings, strange encodings, or ambiguous failure responses, not with neat sample data.</figcaption>
</figure>

<p class="bt4k-post-meta">2026-06-28 · bluetape4k-dependencies 1.3.0 in Practice · Part 4</p>

Input edges are easy to underestimate. Small files, short strings, normal JSON, and hand-made test data all behave.
Production sends something else:

- an 80MB TIFF
- an image with strange rotation metadata
- a scan where OCR may or may not work
- a 300,000-character search query
- a string crafted to bypass blockword checks
- text with broken encoding

## Image: Check Memory Before OCR

OCR examples are useful, but the first operational question is where the file enters memory.

```kotlin
// Convenient for samples. Risky for large uploads.
val bytes = multipartFile.bytes
val text = ocr.read(bytes)
```

Large-file I/O should make size and streaming limits explicit before OCR options are tuned.

```kotlin
fun extractText(path: Path): OcrResult {
    return fileSystem.source(path).buffer().use { source ->
        imageReader
            .read(source)
            .resize(maxWidth = 2_000)
            .normalizeForOcr()
            .runOcr(language = "kor+eng")
    }
}
```

The request path needs limits at every stage.

<figure class="bt4k-architecture" data-diagram-title="Input Boundaries and Resource Limits">
  <img src="/assets/bluetape4k-dependencies-input-boundary-01-en.png" alt="Input boundary flow from request receipt through low-cost checks, bounded decode, OCR or text processing, and a safe result, with invalid input rejected early" loading="lazy" />
  <figcaption>The boundary begins when the request arrives, not after decode. Check size and format first, then admit only bounded work.</figcaption>
</figure>

| Stage | Limit to apply |
|---|---|
| upload | request size, content type, timeout |
| temporary file | disk quota, cleanup policy |
| decode | supported formats, maximum pixel count |
| preprocess | resize limit, native runtime failure handling |
| OCR | language, timeout, empty-result handling |

## API Responses Should Not Hide Failure

An OCR API should distinguish user input problems from server/runtime problems.

```kotlin
sealed interface OcrFailure {
    data object PayloadTooLarge : OcrFailure
    data object UnsupportedFormat : OcrFailure
    data object TimedOut : OcrFailure
    data class RuntimeFailed(val cause: Throwable) : OcrFailure
}
```

| Failure | HTTP status | Reason |
|---|---|---|
| file too large | `413` | The request exceeded an input limit. |
| unsupported format | `415` | The client must change the input. |
| OCR timeout | `504` or retryable failure | Processing exceeded its limit. |
| native runtime error | `500` | The server environment or native library needs inspection. |

## Text: Put Length Limits Before Tokenizers

Tokenizers and language detectors are often attached behind HTTP endpoints. That makes input length a service policy,
not only a library detail.

```kotlin
data class TextRequest(val text: String)

fun TextRequest.validate() {
    require(text.length <= 10_000) { "text is too long" }
}
```

The expensive work should run after validation.

```kotlin
val normalized = normalizer.normalize(request.text)
val language = lingua.detect(normalized)
val tokens = koreanTokenizer.tokenize(normalized)
```

For search APIs, the empty or unresolved case is also a contract. Do not pretend language detection succeeded when it
did not.

## Blockword Checks Need More Than Pass/Fail

A blockword API usually starts as a boolean:

```kotlin
val allowed = blockwordChecker.isAllowed(input)
```

Production policy needs more questions:

- Which fields are checked?
- Is the original text logged?
- Does the user see which word matched?
- Are bypass strings normalized first?
- How does an administrator update dictionaries?

The safer shape carries a decision and a redacted response boundary.

```kotlin
data class BlockwordDecision(
    val allowed: Boolean,
    val matchedPolicy: String?,
    val reasonCode: String,
)
```

## Input Boundary Checklist

| Boundary | Question |
|---|---|
| request size | What is the maximum size this endpoint accepts? |
| decode/tokenize timeout | Where does long processing stop? |
| memory | Where does input become a full `ByteArray` or `String` copy? |
| status code | How are user input problems separated from server problems? |
| error response | Does raw input leak into responses or logs? |
| normalization | Is OCR/text/blockword normalization required before checking? |
| observability | Can payload-too-large, timeout, and validation failures be seen as metrics? |

## Closing

`dependencies 1.3.0` makes Image and Text APIs more usable, but the important lesson is still at the service edge.
Before OCR quality or tokenizer accuracy, decide how large input can be, when processing stops, what gets logged, and
which failure belongs to the client versus the server.

## Series

If you want the first import path, start with the [usage guide](/blog/bluetape4k-dependencies-usage-guide/).

- [bluetape4k-dependencies 1.3.0 in Practice Part 1: Features and Cleanup](/blog/bluetape4k-dependencies-1-3-0-library-stories/)
- [bluetape4k-dependencies 1.3.0 in Practice Part 2: Composing Services](/blog/bluetape4k-dependencies-1-3-0-service-composition/)
- [bluetape4k-dependencies 1.3.0 in Practice Part 3: Production Signals](/blog/bluetape4k-dependencies-1-3-0-production-signals/)
- [bluetape4k-dependencies 1.3.0 in Practice Part 4: Input Boundaries](/blog/bluetape4k-dependencies-1-3-0-input-boundaries/)

## Resources

- [`bluetape4k-image 0.3.0` CHANGELOG.md](https://github.com/bluetape4k/bluetape4k-image/blob/0.3.0/CHANGELOG.md)
- [`bluetape4k-image` issue #165](https://github.com/bluetape4k/bluetape4k-image/issues/165)
- [`bluetape4k-image` issue #171](https://github.com/bluetape4k/bluetape4k-image/issues/171)
- [`bluetape4k-text 0.2.1` CHANGELOG.md](https://github.com/bluetape4k/bluetape4k-text/blob/0.2.1/CHANGELOG.md)
- [`bluetape4k-text` issue #99](https://github.com/bluetape4k/bluetape4k-text/issues/99)
