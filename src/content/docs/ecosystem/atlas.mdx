---
title: Ecosystem Atlas
description: Navigate bluetape4k through Build, Learn, and Apply, then drill into every bluetape4k-projects module manual.
---

import EcosystemAtlas from '../../../components/EcosystemAtlas.astro';
import ProjectsManualMap from '../../../components/ProjectsManualMap.astro';

Kotlin/JVM is the primary map. Read it from left to right: **Build** contains reusable libraries, **Learn** contains runnable workshops, and **Apply** contains reference applications and benchmarks. Select a location to reveal its real repository and workshop connections.

Go, Rust, and Python are independent sibling ecosystems. They appear in their own language tracks below the Kotlin/JVM map instead of being mixed into the same journey.

<EcosystemAtlas locale="en" />

## bluetape4k-projects: repository → group → module → manual

The repository manual is deeper than the README: every registered Gradle module has task-oriented guidance, coordinates, source and test anchors, failure behavior, operations notes, and a Korean counterpart.

<ProjectsManualMap locale="en" />
