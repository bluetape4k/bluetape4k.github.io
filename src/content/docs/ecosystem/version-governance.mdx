---
title: Version Governance
description: How bluetape4k keeps dependency versions aligned across repositories.
---

## Central Rule

`bluetape4k-dependencies` is the source of truth for shared dependency versions across the organization.

Downstream repositories should consume the central BOM and generated shared-version catalog updates instead of allowing independent Dependabot bumps for centrally governed libraries.

## Current Published Baseline

| Scope | Maven coordinate | Version |
| --- | --- | --- |
| Shared third-party dependency governance | `io.github.bluetape4k:bluetape4k-dependencies` | `1.1.3` |
| Core bluetape4k library modules | `io.github.bluetape4k:bluetape4k-bom` | `1.9.1` |
| Exposed JDBC/R2DBC extensions | `io.github.bluetape4k.exposed:bluetape4k-exposed-bom` | `1.9.1` |
| AWS SDK wrappers | `io.github.bluetape4k.aws:bluetape4k-aws-bom` | `0.2.1` |
| Graph database integrations | `io.github.bluetape4k.graph:bluetape4k-graph-bom` | `0.4.1` |
| Leader election modules | `io.github.bluetape4k.leader:bluetape4k-leader-bom` | `0.2.1` |
| Image processing modules | `io.github.bluetape4k.image:bluetape4k-image-bom` | `0.1.2` |
| JaVers audit/diff modules | `io.github.bluetape4k.javers:bluetape4k-javers-bom` | `0.1.2` |
| Text processing modules | `io.github.bluetape4k.text:bluetape4k-text-bom` | `0.1.2` |

Import `bluetape4k-dependencies` first, then `bluetape4k-bom`, then any
repository-specific BOMs required by the selected modules.

## Governed Families

- Kotlin, kotlinx, Dokka, and Kover
- Spring Boot 3/4, Spring dependency management, Jackson 2/3
- JetBrains Exposed, Ktor, Reactor, R2DBC drivers
- AWS SDKs, Redis clients, Kafka, Testcontainers
- Common build and runtime libraries used across multiple bluetape4k repositories

## Upgrade Policy

Routine patch upgrades belong in the central version source first.

Breaking or compile-risk upgrades get their own issue and migration PR. Current examples:

- MyBatis Dynamic SQL 2.x migration
- Timefold Solver 2.x migration

## Local Verification

```bash
scripts/sync-shared-versions.py --workspace .. --check --summary
scripts/sync-dependabot-ignores.py --workspace .. --check --summary
./gradlew compileTestKotlin --no-daemon
```

Use central sync checks before merging downstream dependency governance PRs.
